# Frontend (React + Vite)

Run locally:

```bash
# from project root
cd frontend
# install deps (npm or yarn)
npm install
npm run dev
```

The dev server runs on `http://localhost:5173` by default. The frontend talks to the backend at `http://127.0.0.1:8000` — set `VITE_API_URL` to change.
